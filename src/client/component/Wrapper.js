// library
import React from 'react';

// components

// scss
import '../scss/wrapper.scss';

export const Wrapper = () => {
  return (
    <div className="wrapper container">
      <div>wrapper</div>
    </div>
  );
};